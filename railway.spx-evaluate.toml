# Railway config for the SPX-Engine-Evaluation cron service.
# Wire it up: service Settings -> Config-as-code -> set path to "railway.spx-evaluate.toml"
# Requires CRON_SECRET on this service (same value as blackout-web).
[build]
builder = "nixpacks"
buildCommand = "echo 'cron trigger service: no app build needed'"

[deploy]
startCommand = "node scripts/hit-cron.mjs /api/cron/spx-evaluate"
cronSchedule = "*/5 11-19 * * 1-5"
restartPolicyType = "never"
numReplicas = 1
