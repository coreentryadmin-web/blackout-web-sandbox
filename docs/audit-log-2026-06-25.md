# BlackOut market-hours live-data audit log — 2026-06-25

### 06:10:16 PDT — cron cycle (time-gated)
- MARKET: CLOSED — I:SPX price=0, stale age (no live ticks; pre-open/off-hours). Time-gate → light infra check only; full data cycle deferred to RTH.
- INFRA: ✅ GREEN — health_ok=true, market_health_ok=true | UW limiter: 0 recent-429s, circuit closed, 0 in-flight | Polygon: 0 consec-429, circuit closed | Redis: not degraded | polygon-indices WS: OPEN | route_errors: 0.

### 06:42:50 PDT — cron cycle (MARKET OPEN / RTH)
- MARKET: ✅ OPEN — I:SPX 7397.68 fresh, I:VIX 18.15 ticking.
- HEALTH: ⚠️ health_ok=FALSE — 5 warnings/0 critical: I:TICK/I:TRIN/I:ADD breadth stale·zero; UW gex + UW net_flow WS = CONNECTING. Polygon provider healthy (577 calls/5m, 0 err, 0 rate-limits).
- ❌ #74 VALUATION FAIL: all 3 dummy positions still 'unavailable' ~10min into RTH (no mark/P&L/greeks).
- ❌❌ ROOT CAUSE (HIGH/blocker): market-hours crons STALE — Night's Watch Warm last-ran 10:01 UTC (~3 AM PT), Flow Ingest 10:01, UW Cache Refresh 10:02 — all STALE during RTH while SPX Engine is healthy (13:40 UTC, firing). Specific Railway cron trigger services stopped firing ~10:01 UTC. Cascade: breaks position valuation (#74), HELIX flow freshness (Flow Ingest), UW data (UW Cache Refresh + UW WS CONNECTING). Night Hawk Edition still failed (#77).
- ✅ LIMITERS: UW 0 429s/circuit closed/0 in-flight; Polygon 0 429s/circuit closed; Redis not degraded; route_errors 0.
- ACTION: OPERATOR must verify/restart Railway cron trigger services for nights-watch-warm, flow-ingest, uw-cache-refresh (died ~10:01 UTC). Not fixable from code (Railway config / CRON_SECRET). RE-CHECK next cycle.

### 06:49:35 PDT — HEATMAP DATA STALENESS (manual spot-check)
- ❌❌ Heatmap GEX data FROZEN: /api/market/gex-positioning spot=732.75 (flip 737.05, call_wall 750, put_wall 730) vs LIVE SPX 7397.68 (SPY≈739.8). ~7pts stale — frozen at the ~3 AM PT warm snapshot. Confirms #90: the GEX cache hasn't re-warmed since uw-cache-refresh/heatmap-warm died at 10:01 UTC. Heatmap numbers are NOT updating live. Same operator/Railway fix (#90).

### 08:58:28 PDT — cron cycle (MARKET OPEN / RTH) — ⭐ RECOVERY CONFIRMED
- MARKET: ✅ OPEN — I:SPX 7371.48 fresh & ticking.
- ✅✅ #90 RESOLVED (root cause = Railway Config-as-Code NOT wired per service, not a missing schedule). Operator (in browser, with me driving) created heatmap-warm + Night's-Watch-Warm-New, wired config-as-code on all frozen services. VERIFIED LIVE in Railway: heatmap-warm (every 1min) + Night's-Watch-Warm-New (every 1min) + UW-Cache-Refresh-New (every 2min, ran 200) + Cron-Staleness-Watchdog (every 20min, full history) — all show "Next in X" + CRON_SECRET present + latest code. heatmap-warm previously DID NOT EXIST → that was why GEX never auto-warmed. Warm-keeper stopgap retired.
- ✅✅ GEX-SPOT-FRESHNESS FIX VERIFIED (commit 111c271, deployed): SPX gex-positioning spot=7371.48 == live WS SPX 7371.48 EXACTLY (was ~7pts stale at 06:49). resolveSpotSnapshot now overlays the live WS index price. SPY gex spot 734.0 == ground-truth Polygon SPY 734.26 (~0.3, real-time).
- ✅✅ #74 RESOLVED: dummy positions VALUE live — SPY730P +$58.50, SPY735C −$73 (status 'live', greeks/mark/P&L populate). QQQ710C correctly 'unavailable' with NEW reason field 'contract-not-found' (commit df4a4da) — it's an unlisted contract, NOT a bug. Valuation engine proven.
- ✅ CROSS-TOOL (partial): SPX walls 7500/7300 == SPY 750/730 ×10 (put wall, call_wall API consistent). flip/put_wall/max_pain agree UI↔API.
- ⚠️ NEW FINDING (P1, cross-tool #80-class): SPY CALL WALL divergence — Heat Maps UI Key-Levels box shows CALL WALL=737 ("Resistance 737"), but /api/market/gex-positioning (consumed by Largo get_positioning + Night's Watch) returns call_wall=750. flip(736.6≈736)/put_wall(730)/max_pain(740) all MATCH; only call_wall (+net_gex: UI −549.8M vs API −751M) diverge. PERSISTENT (API said 750 at 06:49 frozen AND 08:58 live → definitional, not timing). A trader sees 737 on Heatmap but Largo/NW would cite 750. Deep-pass workflow wf_688fea8a investigating root cause (two code paths: heatmap matrix wall calc vs gex-positioning summary calc) → converge to one definition.
- ✅ LIMITERS: UW 0×429, circuit closed; Polygon clean. health_ok=FALSE still (same 5 non-critical: I:TICK/I:TRIN/I:ADD breadth + UW gex/net_flow WS CONNECTING — separate feeds, 0 critical).
- 5 code fixes deployed today (8c2ec35..885eb55): GEX spot freshness, NW valuation diagnostics+reason+WS-boot, cron watchdog self-heal/alert, number-format (GexDealerPanel null-paint + NW moneyCompact sign), embed cross-origin framing. Build green, pushed, deploy verified (embed frame-ancestors * live).
- PENDING: #73 Largo grounding (interactive check deferred to next cycle), #77 Night Hawk Edition, #81 VITALS Ph2, the call_wall convergence (pending workflow synthesis).

### 09:1X PDT — cycle-1 deep-pass synthesis (wf_688fea8a, 4 agents, 10 findings)
- ✅ call_wall 737-vs-750 = NOT A BUG (de-escalated my own ⚠️): the Heatmap key-level box (GexHeatmap.tsx:2506), the "Resistance" regime text (polygon-options-gex.ts:704), and /api/market/gex-positioning (gex-positioning.ts:115/150) ALL read the SAME field off the SAME cache key gex-heatmap:{root} (20s TTL) — byte-identical within one payload, cannot disagree by definition. The observed gap was TEMPORAL (two reads ~20s apart on a wall moving in an active market) + me conflating the MAGNET (737/738 = argmax|net_gex|, near spot/flip) with the CALL WALL (750 = argmax +net_gex). Minor P2 only: surface `asof` + disambiguate magnet-vs-wall labels in the box.
- 🚩 P0 FLAGGED (NOT auto-fixed — needs a product decision): cross-TOOL GEX SCOPE divergence is structural. SPX desk + Largo get_positioning + Night Hawk compute GEX over a SINGLE 0DTE expiry (fetchPolygonOdteDeskBundle / fetchPolygonPositioningBundle, expiry=todayEtYmd, polygon-options-gex.ts:163) while Heat Maps + canonical getGexPositioning() sum ~8 expiries → same-named metrics (Net GEX, gamma flip, Magnet, walls) disagree for SPX/SPY by construction. Sub-issues: (a) Largo get_positioning bypasses canonical getGexPositioning → calls fetchPositioningSummary = ROOT CAUSE of #73 (run-tool.ts:1216-1218); (b) two flip algos coexist (computeZeroGammaFlip canonical vs computeGammaFlip cumulative, gamma-desk.ts:54); (c) topGexWalls (gamma-desk.ts:144-168) labels walls by GEOMETRY-vs-spot, NOT net_gex-sign = #80-residual. FIX deferred for review: route SPX/Largo/NightHawk through all-expiry getGexPositioning() OR explicitly relabel 0DTE tiles. Ambiguous (is the desk's 0DTE scope intentional for intraday trading?) → flagged, not guessed mid-market. #73 description updated with the precise root cause.
- 🔧 P1 FIXED (commit 1df9528, tsc green, build-gating → push): NightsWatchPanel off-live card (NightsWatchPanel.tsx:611) hardcoded "Valuation resumes at market open" for EVERY non-live position → an unlisted contract (reason 'contract-not-found', e.g. the QQQ710C test position) showed a FALSE market-open promise on the card while the detail modal correctly said "unlisted contract" — card contradicted its own modal. Now contract-not-found → "Unlisted contract · no live option chain"; market-closed/no-quote/pending keep the resumes-at-open empty state (no #82 after-hours regression).
- CYCLE-1 VERDICT: site is materially REAL & healthy on live data — recovery from the 6:42 broken state fully confirmed. One clear UI-honesty bug fixed; one structural cross-tool scope issue flagged for product review (#73). Next cycle: interactive Largo grounding test, SPX-desk + HELIX visual passes, re-verify values still tracking.

### 09:17–09:22 PDT — CYCLE 2 (MARKET OPEN / RTH)
- TICKING ✅: live SPX 7371.48→7388.37→desk 7382.01 (moving, ~6pt intraday range across fetch times); VIX 18.6–18.7; gex spot tracks live (7387.85 vs 7388.37, ~0.5). Call wall drifted 750→738 → CONFIRMS cycle-1 "737-vs-750" was temporal (wall moves intraday), de-escalation correct.
- ✅ POSITIONS valuing live w/ SENSIBLE P&L MOVES: as SPX dipped, SPY735C 735C call −73→+9.5→−$11.00, SPY730P put +58.5→+12→+$19.50 (directionally correct: call bleeds / put gains on a dip). NW header total +$8.50 == exact sum of the 3 cards. Greeks/B-E/DTE/OI/IV all populate; verdicts grounded ("expiry-worthless risk: 1DTE OOM", "theta 35% of mark/day at 1DTE").
- ✅✅ NW UNLISTED-CARD FIX DEPLOYED & WORKING (commit 1df9528 live): QQQ710C card now reads "Unlisted contract · no live option chain / can't judge this position yet" — NO longer the false "resumes at market open". Matches its detail modal.
- ✅ SPX DESK /dashboard: every value populates — px 7382.01/+0.33%, VIX 18.69, VWAP 7375.76, EMA 20/50/200 (7381.58/7377.75/6904.93), SMA 50/200 (7357.35/6920.90), HOD 7419/LOD 7323.5/PDH 7428.06/PDL 7336.82, regime bullish, IV rank 69, max pain 7410, 0DTE GEX walls 7370-7395 w/ $-values + pt-distances, movers, live tape, Largo LIVE. Only "—" = Γ FLIP (honest: 0DTE flip undefined, "γ unknown" — not a bug) + closed-plays empty states (correct: no plays yet today).
- ✅ LARGO LIVE genuinely GROUNDED: "SHORT 7384.19, into 0DTE GEX wall 7385 ($2.1M), grade C, neg-γ" — levels R 7385/7395, S 7375/7370, pin 7410(maxpain) all match the desk's 0DTE nodes. Coherent, ticker-correct, numerically grounded.
- ⚠️ P0/#73 CONFIRMED LIVE (side-by-side evidence): SPX desk GEX −$27.0M (0DTE) vs Heatmap NET GEX −$363M (all-expiry); maxpain 7410 vs 7450; Γ flip "—" vs 7384.78; walls 7370-7395 vs cw7450/pw7300. Largo cites the DESK's 0DTE levels → agrees with desk, DIVERGES from heatmap = #73 root cause, observed live. NOTE: desk is BRANDED "0DTE COMMAND DESK" + walls labeled "0DTE NODES" → 0DTE scope is likely INTENTIONAL → fix = clarify/relabel scope on the top GEX/maxpain/flip tiles + route Largo to the right scope per question, NOT a blanket re-route to all-expiry. Still flagged for product decision (#73).
- ⚠️ MINOR (P2): SPX desk live-tape shows a dark-pool print stamped "4:20 PM" at 09:21 AM — possibly a stale/yesterday print or a TZ/format artifact in the tape timestamp; verify next cycle.
- ✅ INFRA: UW 0×429 circuit-closed; health_ok=FALSE = same 5 non-critical (breadth + UW WS). 
- ACTIONS: deep-pass cycle-2 workflow wf_58ba77e6 running (number-format / mock-trade-lifecycle+input-validation / security-sovereignty) → synthesis pending.

### 09:32 PDT — cycle-2 deep-pass synthesis (wf_58ba77e6, 4 agents, 7 findings, NO P0s, all NEW)
- P1 INPUT-VALIDATION (auto-fix, batching): (a) isValidYmd accepts IMPOSSIBLE dates — Date.parse rolls 2026-02-30→03-02, 2026-06-31→07-01 (route.ts:20-24 + [id]/route.ts:24-27); bad expiry → Postgres DATE INSERT fails → 502 not 400, and DTE math (valuation.ts:287-293) uses the rolled date. (b) POST accepts PAST/WEEKEND expiry → position pins dte=0, never matches a live contract, stays unavailable/contract-not-found — THE SATURDAY-EXPIRY CLASS that caused the earlier #74 false alarm. Fix: shared validateExpiryYmd (toISOString round-trip == input + reject < todayEt; optional Sat/Sun listing_warning) in both routes.
- P2 NUMBER-FORMAT (auto-fix, batching): (c) fmtPrem (format.ts:148-152 — raw finding wrongly cited api.ts) + fmtFlowPremShort (flow-strike-stacks.ts:155-160) put minus INSIDE the $ glyph + no finite-guard; fmtPrem output feeds the overnight LLM prompt. (d) polygon.ts:314-315 defaults missing mover price/change% to 0 → SectorThermal:66-68 prints fabricated "0.00 / 0.00%" reading as a flat real quote. Fix: sign-outside + dash-for-non-finite; display-guard the fabricated-0 movers (at SectorThermal to avoid the getTopMovers ripple).
- 🚩 FLAGGED (product/edge, NOT auto-fixed): (e) REALIZED P&L for CLOSED positions — closeUserPosition stores exit_premium but enrichPosition runs null-valuation on close → closed card shows a DASH (run-tool.ts:1284-1309 emits null pnl). Math clear ((exit−entry)*contracts*100*sideSign + pct) but label / pct-denominator / short-cost-to-close = product decision. (f) expiry guard covers past+Sat/Sun only, NOT market holidays. (g) two isValidYmd copies = drift risk → extract shared helper.
- ✅ SECURITY/SOVEREIGNTY: no new P0 (no secret-in-client, no PII-in-URL, gating intact, admin routes authed).
- PLAN: batch these auto-fixable findings + the numericals-audit (wf_d3236259) confirmed bugs into ONE coordinated disjoint-cluster fix pass → tsc+build+push.

### 09:40 PDT — DEEP NUMERICALS AUDIT (user-requested; wf_d3236259, 6 agents, 51 checks/5 domains)
- ⭐ VERDICT: numerically SOUND on every metric that drives trader decisions — 38 VERIFIED-OK with formulas cited (all GEX/VEX/DEX/CHARM signs, wall/flip/max-pain argmax, Wilder RSI, EMA α=2/(n+1), SMA, breakeven, long/short unrealized P&L, net-premium, OTM%). Also ground-truthed 5 random heatmap spots vs Polygon: all within 0.05-0.17% (= the ~20s cache window). Random-ticker net-GEX SIGN tracks spot-vs-flip across 8 tickers (IWM/AMD spot>flip → +GEX; NVDA/AAPL spot<flip → −GEX). HEATMAP CADENCE: GEX+spot refresh every ~20s-1min (cache TTL + warm cron), NOT per-second — verified (7s re-pull = byte-identical) — correct/by-design for a structural metric.
- 🔴 #91 P0 (FLAGGED, grep-confirmed): SPX desk EMA 20/50 = 20-MIN/50-MIN (fetchIndexEma 'minute', spx-desk.ts:844-845) but EMA 200/SMA 50/200 = DAILY, all under uniform labels → fabricated proximity; inferRegime(price,ema20,ema50) reads off the 20m-vs-50m cross. Values real → labeling/design decision (disclose timeframes vs all-daily). Task #91.
- 🟡 #92 P1 (FLAGGED): GEX dollar-gamma missing spot*0.01 → every $-magnitude ~60x understated for SPX vs SpotGamma standard. NON-decision-material (sign/walls/flip/regime/maxpain/King all scale-invariant + verified correct) — but headline $ won't match Barchart/SpotGamma. Fix ready (×spot*0.01 at polygon-options-gex.ts:1760,2232); flagged because it's a 60x visible change to coordinate + VEX/CHARM consistency decision. Task #92.
- 🟡 #93 P1 (FLAGGED): "IV Rank" label is IV PERCENTILE on the primary Polygon path (polygon.ts:734-735) and a true RANK only on UW fallback — same label, two defs diverging by tens of pts. Task #93.
- 🟡 #94 P1 (FLAGGED): closed positions show NO realized P&L (dash) though exit_premium is stored — math clear, UX is a product decision. Task #94.
- 🔧 AUTO-FIXING NOW (wf_c75f785d, 3 sequential disjoint clusters, tsc-gated, no-push→I build+push): (1) input-validation — shared validateExpiryYmd (reject impossible dates via ISO round-trip + past expiries; 400 not 502) in positions/route.ts + [id]/route.ts [the Saturday-expiry class]; (2) ma-fallback+dte-tz — polygon.ts indexClosesAsc explicit limit so 50d/200d MA fallback can seed (was default '10' < window → always null), + db.ts DTE uses ET not UTC CURRENT_DATE (fixes 8pm-midnight-ET off-by-one); (3) number-format — fmtPrem (format.ts:148) + fmtFlowPremShort (flow-strike-stacks.ts:155) sign-outside-glyph + finite-guard, + SectorThermal display-guard for fabricated-0 movers.
- P2/edge (logged, not yet actioned): UW-fallback GEX scale discontinuity, dark-pool size→$ units, index-VWAP equal-weight proxy, cross-surface staleness-window mismatch, live-spot read-path overlay (the per-second-heatmap-spot enhancement), 0DTE-vs-all-expiry scope (#73).

### 09:55 PDT — DECISIONS #91-94 IMPLEMENTED (user delegated; decided on convention + integration grounds)
- 3 contained fixes from the audit shipped first (1df9528..7c451a2: input-validation, ma-fallback+dte-tz, number-format — build-green, pushed, deploying).
- #91 (f30ee40): DECISION = all SPX desk MAs DAILY. EMA 20/50 switched 'minute'→'day' at both call sites (spx-desk.ts:844-845, 1148-1149) → coherent with EMA200+SMA50/200; inferRegime now a stable daily-trend (vs noisy 20m/50m cross); labels 'EMA 20/50/200' now accurate. Rationale: EMA 20/50/200 = daily on every trader platform; VWAP+0DTE walls already cover intraday. tsc green.
- #92 (63a19a0): DECISION = APPLY the SpotGamma per-1%-move standard. GEX dollar-gamma ×spot*0.01 (total spot^2*0.01) at polygon-options-gex.ts:1760,2232. VERIFIED no absolute-$ consumer exists (walls=sign, King=argmax, heatmap color=per-matrix-peak-relative, flip=sign, gex-alerts=event-type, verdict=kind+distance, 1e9/1e6/1e3=display formatters) → the ~60x magnitude correction changes ZERO signals/decisions/colors, only aligns the headline $ to SpotGamma/Barchart. VEX/CHARM left on their notional convention + commented. tsc green. Closes #92.
- #93 (84591a0): DECISION = TRUE IV Rank on the primary path. fetchVixIvRankPercentile now computes (cur-min)/(max-min)*100 (guarded), matching the 'IV Rank' label + the UW true-rank fallback (TastyTrade convention). tsc green. Closes #93.
- #94 (713b93d): DECISION = implement Realized P&L. realized_pnl=(exit-entry)*contracts*100*sideSign + realized_pnl_pct (entry-cost denom) on EnrichedPosition — SAME conventions as unrealized; closed card renders 'Realized P&L' (emerald/bear, sign outside); flows to Largo via run-tool. Close route/enrichment needed no change (closed row flows through enrichPosition). tsc green. Closes #94.
- #91 remains a follow-up only IF the user wants the desk to be explicitly intraday instead (decided daily; reopen if product intent differs).
- STATUS: 4 commits (f30ee40..713b93d) build-gating (b3suc9wwi) → push on green. This audit session (cycles 1-2 + user numericals ask) shipped 10 fixes total; core math proven sound (38 verified-OK); 0 open P0s after these.

