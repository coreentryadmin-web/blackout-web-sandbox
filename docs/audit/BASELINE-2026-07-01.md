# Data-Validation Baseline — 2026-07-01 (pre-open / last-close)

Captured ~07:20–07:40 UTC (market **closed**, pre-market) via an authenticated
admin/premium session. Numbers are prior-session close (2026-06-30). The
market-open run should diff **live RTH** values against this and against
Polygon/UW ground truth. All temp Clerk users were deleted and verified gone.

## Cross-provider validation result (captured app vs live Polygon+UW)
Totals: **13 PASS / 7 INFO / 19 WARN / 0 FAIL**

### ✅ Confirmed correct
| Quantity | App | Ground truth | Δ |
|---|---|---|---|
| SPY spot (`quote.price` & `gex.spot`) | 746.77 | Polygon close 746.77 | **0.000%** |
| SPX (`indices.spx.price`) | 7499.36 | Polygon I:SPX close 7499.36 | **0.000%** |
| SPX/SPY ratio | 10.042 | ~10 | ✓ |
| GEX walls | put 735 < flip 740.63 < spot 746.77 < call 750, max_pain 740 | ordering valid | ✓ |
| gamma/dex/vanna posture | long / short / positive | matches sign of net_gex(+3.06e9)/net_dex(−7.63e9)/net_vex(+4.48e10) | ✓ |
| app `gex_cross_validation` | callWallMatch/putWallMatch/flipMatch = true, divergence 0.63 | vs UW | ✓ |
| Track record arithmetic | 0+9+0 = 9; win_rate 0 = round(0/9) | consistent | ✓ |

### ⚠️ Findings to watch / fix
1. **Systemic unrounded-float noise** — 16 of 65 payloads serve values like
   `7499.360000000001`, `ema20=7428.6691886260705` (13 dp), `net_gex=3062180849.185327`,
   heatmap cell `-465096.837671076`. Affects: `market/indices`, `gex-positioning`,
   `gex-heatmap`, `spx/desk|merged|signals|play|outcomes`, `platform/snapshot`,
   `market/flows`, `nighthawk/edition`, `grid/bootstrap`, `admin/analytics|spx|signal-analytics`,
   `track-record/plays`. Values are ~correct but **malformed for display** — round at the data layer (prices 2dp, EMAs/levels a fixed precision).
2. **VIX source/freshness** — app `17.18` vs Polygon prior-close `16.45` (4.4%). App shows prior-close for SPX/SPY but a *newer* VIX → confirm at open with same-timestamp live compare (may be a different VIX feed).
3. **Track record: 0 wins / 9 losses (0% public win-rate)** — arithmetic correct; verify the outcomes/settlement pipeline isn't mis-scoring wins as losses vs a genuine 2-day losing sample.
4. **`net_gex` sign vs raw UW greek-exposure** — differ (app +, UW SPY call+put gamma −); expected units/convention difference (app self-validation passes), noted so the morning run doesn't over-flag it.

## Live-crawl signals (91 targets, all pages HTTP 200)
- `/api/signals/open` → **401 even with admin session** (investigate auth guard).
- `/api/nighthawk/play-status` → **404**; `/api/market/largo/session` → **400**.
- Slow (5–7.6s): `grid/bootstrap`, `admin/health`, `market/lotto/today`, `admin/spx/dashboard`, `market/flow-brief`.
- Generic `<title>` on `/learn/glossary`, `/learn/heat-maps`, `/learn/nights-watch`, `/embed/track-record`; sign-in/up titled "HELIX".
- Ops alerting off: `discord_ops_webhook=false`, `discord_play_webhook=false` (AI spend kill-switch armed at $250).
- Cron: 23 jobs healthy, 0 stale/failed. Launch: spx/flows/heatmap/nighthawk/grid live; largo locked.

## Provider ground-truth snapshot (2026-06-30 close)
- Polygon: SPY 746.77 (H 748.02 / L 740.89), I:SPX 7499.36, I:VIX 16.45, market=closed.
- UW market-tide (2026-06-30): net_call_premium 419,074,751; net_put_premium −18,288,445; net_volume 1,655,753.
