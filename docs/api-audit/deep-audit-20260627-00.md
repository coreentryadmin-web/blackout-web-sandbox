# BlackOut Deep Platform Audit
**Run time:** 2026-06-27 00:12 ET (Saturday — market CLOSED)
**Deployed HEAD:** `e8e0908` (== origin/main)
**Overall Risk:** LOW
**Issues found:** 0 P0 | 0 P1 | 2 P2 | 1 P3 (audit-tooling)

> Market context: today is **Saturday**. Equities/options are closed, so RTH data-freshness
> checks (SPX desk, HELIX tape) do **not** apply. All data endpoints correctly return `401`
> (auth-gated) to anonymous probes, and weekday-only crons correctly show "next run in 2 days".

---

## P0 — Fix Immediately (User-Facing Breakage)
**None.** Deployed production is healthy across every surface checked.

## P1 — Fix Today (Feature Broken or Degraded)
**None confirmed.**

> The raw audit script reported 4 P1 "404" failures (SPX Pulse, Flows, Night Hawk, Grid News).
> **All four are false positives** caused by stale endpoint paths in the audit SKILL itself —
> see P3-1. The real routes exist and respond `401` (auth-gated by middleware), which is correct.

## P2 — Fix This Week (Wrong But Not Visible)

### P2-1 — Uncommitted WIP pile contains 7 real TypeScript errors that would break the Railway build if committed
A coherent body of feature work is sitting **untracked** in the working tree (never committed):
- `src/app/api/platform/` (incl. `platform/intel/route.ts`)
- `src/app/api/brief/`, `src/app/api/coaching/`, `src/app/api/track-record/`
- `src/app/api/market/anomalies/`, `src/app/api/market/regime/`
- `src/app/api/admin/run-migration/`, `src/lib/migrations/`, `src/lib/run-migration.ts`

`src/app/api/platform/intel/route.ts` has **7 genuine type errors** (`.map()` callbacks annotated
with concrete shapes over `QueryResultRow[]`, which TS rejects):
- `intel/route.ts:83`, `:84`, `:92`, `:141` — `error TS2769: No overload matches this call`
- `intel/route.ts:111`, `:121`, `:147` — `error TS2345: callback not assignable to QueryResultRow map`

**Why it matters:** `next.config` ignores **eslint** during builds but **not** TypeScript
(`next.config:86` has only `eslint: { ignoreDuringBuilds: true }`; no `typescript.ignoreBuildErrors`).
So if this WIP is committed as-is, the next Railway build **fails**, red-lining every service.
Today it's invisible only because the files are untracked and the deployed build (`e8e0908`) is clean.

**Fix:** either finish + type-correct these routes (cast rows to typed shapes, e.g.
`query<AnomalyRow>(...)` so `.rows` is typed, or `(a as AnomalyRow)`), or stash/remove the pile.
Do not commit it untouched.

### P2-2 — `SpxDarkPoolCard` is defined but never imported/mounted (task #97 still open)
`src/components/desk/SpxDeskPanels.tsx:104` exports `SpxDarkPoolCard`, but a repo-wide search finds
**zero** import sites. It renders nowhere. Either mount it on the SPX desk (alongside the sibling
cards consumed by `SpxStructureBlocks.tsx` / `SpxDashboard.tsx`) or delete it as dead code.

## P3 — Audit-tooling reliability (the audit script lies to itself)

### P3-1 — Stale endpoint paths + brittle regexes generate false P0/P1 every run
The audit SKILL probes paths that no longer exist, then flags them as failures:
| Script path (404) | Real path |
|---|---|
| `/api/market/spx-pulse` | `/api/market/spx/pulse` |
| `/api/flows` | `/api/market/flows` |
| `/api/nighthawk/latest-edition` | `/api/market/nighthawk/edition` |
| `/api/grid/news` | (no such panel — news lives at `/api/market/news`; grid = analysts, congress, dark-pool, earnings, economy, movers, sectors) |

Two more script bugs that produce **false "regression" alarms**:
- **db.ts pool handler false-negative:** the script greps `pool\.on(...error)`, but the real handler
  is `livePool.on("error", ...)` at `src/lib/db.ts:110`. Task #100 is **fixed**; the script would
  report it as a P0 regression on every run.
- **TSC check never runs:** `npx tsc` resolves to a stub package (the `tsc` placeholder), not the
  compiler — `node_modules/.bin/tsc` is absent on this Windows install. The script's "0 errors" is
  meaningless. Use `node node_modules/typescript/bin/tsc --noEmit` instead.

**Fix:** update SKILL.md Phase 1 paths, the Phase 2a regex (`livePool?\.on`), and the Phase 2g
TSC invocation.

---

## Resolved since last known backlog (verified fixed this run)
- **#100** pg Pool idle-error handler present — `db.ts:110` (`livePool.on("error")`), pool `max:5`.
- **#101** Clerk `user.created` webhook handled — `src/app/api/webhook/clerk/route.ts:77`.
- **#102** Polygon WS cross-replica leader election (Redis SETNX, 25s TTL) — `src/lib/ws/polygon-socket.ts:117-128`.
- **#73** Largo grounding: `computeSpxConfluence` reachable from `src/lib/largo/run-tool.ts:1211`.
- **SPX plays never opening:** veto neutered — `playOptionChainRequired()` now defaults `false`
  (`src/lib/spx-play-config.ts:404`); `openPlay()` reachable at `spx-play-engine.ts:803`.
- **Redis IPv6:** `family: 0` present — `src/lib/make-redis.ts:58` (+ `reconnectOnError`, `maxRetriesPerRequest`).

## Code hygiene
- **TODO/FIXME/HACK:** 0 real markers in `src/` (the lone `BROKEN` hits are a legit
  `WALL_BROKEN_TICKERS` constant in `cron/gex-alerts/route.ts`).
- **tsconfig:** `strict: true`, `skipLibCheck: true`.
- Of 50 raw `tsc` errors, **41 are local-install artifacts** (missing `@clerk/nextjs`,
  `@anthropic-ai/sdk`, `svix` in local `node_modules` — Railway's `npm ci` resolves them), **9 are
  downstream of those**, and **7 are the real WIP errors in P2-1**. Committed/deployed code is clean.

## Infrastructure pulse (all green)
- `blackout-web`: ● Online · https://www.blackouttrades.com
- Postgres: ● Online · Redis: ● Online
- `NightHawk-Playbook`: ● Online
- Crons: `Flow-Ingest` (*/5, next ~2 min), `SPX-Engine-Evaluation` (*/5 11–20 Mon–Fri — idle on weekend),
  `NightHawk-Outcomes` (Mon–Fri — idle on weekend), `Largo-Chat-CleanUp` (Sun 05:00 — next ~21h). Correct.
- Env-var enumeration skipped: project-scoped `RAILWAY_TOKEN` can't list variables (expected).

## Phase results
- **Live Site:** healthy (Landing/Sign-In/Health 200; all data endpoints 401 auth-gated by design)
- **Code Audit:** 1 P2 (uncommitted WIP type errors) + 1 P2 (dead card)
- **Cross-Tool Consistency:** not testable anonymously (endpoints auth-gated; market closed) — deferred to a weekday RTH run
- **Infrastructure:** all green
- **Backlog:** #73, #97 reviewed → #73 fixed, #97 still open; #100/#101/#102 confirmed fixed
