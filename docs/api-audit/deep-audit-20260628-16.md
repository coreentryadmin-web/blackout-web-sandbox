# BlackOut Deep Platform Audit
**Run time:** 2026-06-28 16:12 ET (Sunday — market closed, RTH freshness checks N/A)
**Overall Risk:** HIGH (one carried P1 infra gap; no new user-facing breakage)
**Issues found:** 0 P0 | 1 P1 (carried) | 2 P2 | 2 P3
**Verdict:** No new user-facing breakage and no data-integrity failures this run. Every #97–#102 code backlog item is confirmed fixed. The one open P1 is carried infra (Market-Regime-Detector writer cron never deployed) — re-confirmed still absent. Other findings are minor (one dormant fail-open auth, one copy/spec mismatch) plus false-alarm sources baked into the audit script.

---

## P0 — Fix Immediately (User-Facing Breakage)
None.

## P1 — Fix Today (Feature Broken or Degraded)
- **Market-Regime-Detector writer cron not deployed (CARRIED, re-confirmed)** — `railway status` this run lists 13 cron jobs; **none is `Market-Regime-Detector`**. The `market_regime` and `flow_anomalies` tables therefore have no live writer → `/api/market/regime` and `/api/market/anomalies` serve stale/empty data during RTH. The `.toml` + handler code exist; the missing step is the manual Railway "add service (Config-as-code)" — deploy-risky, **operator-only** (not auto-created by this audit). Open since the 06-28 08:13/12:23 runs.

## P2 — Fix This Week (Wrong But Not Visible)
- **Fail-open auth on regime write endpoint** — `src/app/api/market/regime/route.ts:48` guards the DB-writing POST with `if (cronSecret && auth !== \`Bearer ${cronSecret}\`)`. If `CRON_SECRET` is ever unset/empty, the guard is skipped and the endpoint becomes a **public injection vector** for `market_regime` rows. Dormant today (CRON_SECRET confirmed present in Railway), but the pattern fails open instead of closed. Fix: reject when `cronSecret` is missing (fail closed). Audit other `if (cronSecret && …)` guards for the same pattern.
- **Grid page promises panels that don't exist** — `src/app/(site)/grid/page.tsx:13` (metadata) and `:35` (subtitle) advertise "**News · Flow · Analysts · Pulse**", but there is no `/api/grid/news` route and the page fetches no news or flow panel (live `/api/grid/news` → 404; actual panels: analysts/catalysts/congress/dark-pool/earnings/economy/movers/sectors/flex). Either wire the News + Flow panels or correct the copy so the page doesn't over-promise.

## P3 — Tech Debt / Cosmetic
- **Stale `.next/types` cache** — `npx tsc --noEmit` reports 3 `TS2307` errors, all inside `.next/types/...` referencing `(site)/learn/layout.js`. The `learn/` directory no longer has a `layout.tsx`, so these are generated-type artifacts from a prior build. **Source compiles clean**; cleared by a fresh `.next` build. No action needed unless CI runs tsc against a stale `.next`.
- **Audit script self-inflicted false alarms** — this scheduled task's own Phase 1/4 use stale paths and a wrong env var name, generating phantom P0/P1 every run: `/api/market/spx-pulse` (real: `/api/market/spx/pulse`), `/api/flows` (real: `/api/market/flows`), `/api/nighthawk/latest-edition` (real: `/api/market/nighthawk/edition`), `/api/grid/news` (no such route), and `UNUSUAL_WHALES_API_KEY` (code uses `UW_API_KEY`, which **is** present). Recommend updating `SKILL.md` so real regressions aren't drowned out.

---

## Backlog verification (#97–#102 + plays)
| Item | Status | Evidence |
|------|--------|----------|
| #100 pg Pool error handler | ✅ FIXED | `src/lib/db.ts:79,113` — `test.on("error")` + `livePool.on("error")`; `max:5` |
| #101 Clerk `user.created` webhook | ✅ FIXED | `src/app/api/webhooks/clerk/route.ts:50` INSERTs users; `webhook/clerk` is an alias re-export |
| #102 Polygon WS replica guard | ✅ FIXED | `src/lib/ws/polygon-socket.ts:117` — Redis SETNX leader election (`polygon:indices:leader`, 25s TTL) |
| #97 SpxDarkPoolCard never imported | ✅ FIXED | imported `SpxDashboard.tsx:13`, mounted `:105` |
| SPX plays never open | ✅ CODE FIXED / ⏳ VERIFY | veto now opt-in — `playOptionChainRequired()` defaults `false` (`spx-play-config.ts:417`); open logic reached at `spx-play-engine.ts:900`. **Verify live Monday RTH** that plays actually open + `spx_play_outcomes` populates |
| Redis IPv6 family:0 | ✅ PRESENT | `make-redis.ts:58` + `reconnectOnError` `:63` |

---

## Phase Results
- **Live Site:** healthy. Landing/sign-in/sign-up → 200; all gated APIs → 401 (correct for unauth). Real endpoint paths confirmed (audit-script 404s were stale paths, not breakage). Weekend → no RTH data-freshness check.
- **Code Audit:** clean. 1 TODO in entire `src/`. TSC source-clean (only stale `.next` cache artifacts). Connection safety (db + redis) intact.
- **Cross-Tool Consistency:** not comparable this run — GEX/pulse endpoints are 401-gated and market is closed; deferred to an RTH run.
- **Infrastructure:** healthy. `blackout-web` Online 5/5 replicas (iad×3, US-West×2); Postgres + Redis Online; all 13 cron jobs scheduled (RTH crons next-run Monday). Required secrets present (`UW_API_KEY`, `POLYGON_API_KEY`, `POLYGON_API_BASE`, `CLERK_SECRET_KEY`, `CLERK_WEBHOOK_SECRET`, `ANTHROPIC_API_KEY`, `CRON_SECRET`, `DATABASE_URL`, `REDIS_URL`).
- **Auth gating:** all paid/market routes self-guard correctly. Only 2 truly-public GET routes (`market/anomalies`, `market/regime`) — both intentionally public with documented justification (market-wide, no per-user/paid data). The single integrity concern is the regime POST fail-open noted above.

## Still pending (carried, not new)
- **P2-C** SPX plays + signal panel: confirm live Monday 2026-06-29 RTH (code fix landed; needs live proof)
- **P2-D** `options-socket` shard 0 `code=1006` reconnect loop — not re-sampled this run (market closed; benign off-hours per prior runs). **Re-check after 09:30 ET Mon 06-29**; climbing `failures` → promote to P1 (Night's Watch valuations degrade). Fix: gate reconnect/heartbeat on options-RTH (`src/lib/ws/options-socket.ts:453-457`).
- #71 Whop pricing
