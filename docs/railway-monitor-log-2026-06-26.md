# BlackOut Railway deploy + cron monitor log — 2026-06-26

Autonomous monitor (`blackout-railway-deploy-monitor`) — watches the Railway dashboard for service crashes, failed deploys, stale/unwired crons (#90 class), and the after-hours edition cron (#77). Cross-checks live-app freshness so a "cron Online but data frozen" trap is caught.

### ~06:30 PDT (09:30 ET, RTH open) — AUTONOMOUS RUN
- ✅ INFRA: no login wall (Railway session valid). **Postgres Online · Redis Online · blackout-web Online** (www.blackouttrades.com) · DB_CLEANUP / Membership-Reconcile / NightHawk-Playbook Online. No failed/crashed services, no red-dot canvas alerts.
- ✅ DEPLOY: current deployment across services = `docs(auto): accessibility-audit 2026-06-26 — 23 WCAG fixes + 5 flagged` (Co-Authored Opus 4.8), deployed ~21–28 min ago, "Deployment successful" everywhere. (This recent monorepo redeploy is what reset the per-deployment cron-run histories — see CRON note below.)
- ✅ CRONS — the #90 check. Drilled into Cron Runs tab for the suspicious "Completed"-badge crons. **ALL confirmed WIRED + scheduled + firing live, not unwired:**
  - **UW-Cache-Refresh-New**: "Next run in 1–2 min", every 2 min 11am–9:59pm M–F; canvas cycled Completed→Next-in-X→**Running (00:00)**→Completed across observations (live firing). Card shows "Last run succeeded".
  - **Flow-Ingest-Cron**: every 2 min; observed in **"Running (00:00)"** state live; "Last run succeeded / Next in X".
  - **SPX-Engine-Evaluation**: every 2 min; observed **"Running (00:00)"** live; "Last run succeeded / Next in X".
  - **Night's-Watch-Warm-New**: "Next run in 7s", every minute 11am–9:59pm M–F; counting down live; "Ready". (Recent Executions "no previous executions" = fresh-redeploy history reset, NOT a failure — schedule active.)
  - **Cron-Staleness-Watchdog**: "Next run in 7 min", every 20 min; **full FRESH execution list today — 6/26 6:21 AM (11m ago), 6:00 AM, 5:41 AM… all green, 1–2s.** The meta-monitor itself ran 11 min ago and succeeded → internal staleness detection is alive and reports nothing stale.
  - **heatmap-warm**: "Completed" between every-minute runs; output (GEX) proven fresh below.
  - Recurring/healthy as before: NightHawk-Outcomes "Last run succeeded / Next in 7h" · Largo-Chat-CleanUp "Last run succeeded / Next in 2 days".
- 🔎 FINDING (false-alarm pattern, documented): The fast crons (UW-Cache-Refresh, Flow-Ingest @ 1–2 min) showed **Recent Executions frozen ~21–22h ago** (newest 6/25 ~9:16/8:25 AM) with a GAP to days prior — which *looks* like the #90 trap. **It is NOT.** Proof: (a) Flow-Ingest, whose UW flow data is provably live (off_lit_trades WS 703 ms old), shows the same 22h-frozen list; (b) the slow 20-min Cron-Staleness-Watchdog shows fresh runs 11 min ago. Conclusion: Railway's **Recent Executions panel is scoped to the current deployment's lineage** — the fast crons redeploy so often (frequent monorepo pushes) that their per-deployment run history resets to stale/empty, while the slow watchdog accumulates visible history between deploys. The **live "Next in X" countdowns, "Running (00:00)" states, and "Last run succeeded" badges are the source of truth** — all green.
- ✅ CROSS-CHECK (live app, RTH, authenticated browser):
  - `/api/admin/health` (gen 13:25:43Z): `health_ok=true`, **0 critical / 0 warning**, issues=[]. Polygon **278 calls/5m, 0 err, 0 ratelimit**, last-200. UW **61 calls/5m, 0 err, 0 ratelimit**, last-200. Rate limiters: UW `circuitOpen=false recent429s=0 degraded=false`, Polygon `circuitOpen=false consecutive429=0`. `redis_degraded=false`, `market_health_ok=true`, `route_errors=[]`. WS: Polygon-indices **OPEN+auth**; all 5 UW channels (flow_alerts/market_tide/off_lit_trades/interval_flow/trading_halts) **OPEN+auth**; Massive options shard **OPEN+auth**.
  - `/api/market/gex-positioning?ticker=SPX`: spot **7357.49, asof 13:26:33Z (live, seconds old)** — NOT frozen. Full desk populated: flip 7404.69, call_wall 7500, put_wall 7300, max_pain 7450, net_gex −27.9B, gamma short, source polygon. Confirms heatmap-warm warmer path is producing live data.
  - ℹ️ `I:SPX price 0` at 13:25Z = **pre-market/at-open** (cash index hadn't ticked yet, ~5 min to/at 9:30 ET open); VIX live at 20.24. Expected, not a fault. (Worth a re-confirm at next RTH run that SPX index price populates intraday — the by-product gex `spot` 7357.49 is already non-zero/correct, so the desk is fine.)
- ⏳ NIGHT HAWK EDITION (#77): **not yet due** — 06:30 PT / 09:30 ET; publish window 17:30 ET (≈2:30 PM PT). Will verify on the after-hours run. (NB: yesterday 6/25 the edition FAILED to publish ~6h past window — watch closely tonight.)
- VERDICT: **Railway GREEN, live data FRESH.** No #90 (every warmer wired + observed firing; the 22h-frozen exec lists are a Railway per-deployment-history display artifact, not a stall — proven via the live watchdog + live downstream data). No failed/crashed services, no login wall. #77 not yet due. Clean run.

### ~07:03 PDT (10:03 ET, mid-RTH) — AUTONOMOUS RUN
- ✅ INFRA: no login wall (Railway session valid; board title "BlackoutTrades.com"). **Postgres Online · Redis Online · blackout-web Online** · DB_CLEANUP / Membership-Reconcile / NightHawk-Playbook / Cron-Staleness-Watchdog Online. No crashed services, no red-dot canvas alerts.
- ✅ DEPLOY: **ACTIVE (serving) deployment = `fix(clerk): preconnect to Frontend API origin to stop UserButton mount-timeout`, 3 min ago via GitHub, "Deployment successful."** History all clean — recent SDLC pushes are `docs(auto): error-triage RUN 3 (cc17d83)` + `fix(nighthawk): harden edition_for date input ::date crash` (REMOVED = superseded, normal), `build-test-gate` SKIPPED ("no changes to watched files"), plus a11y/perf/security commits over prior hours. **No FAILED/CRASHED deploy.** Canvas showed `blackout-web Online · Building (NN:NN)` timer incrementing throughout (02:14→09:23) = the **autonomous-SDLC continuous-push churn** (a build near-always in flight); authoritative Deployments panel stayed on the successful clerk deploy, and live data stayed fresh — cosmetic, not a failure.
- ✅ CRONS — the #90 check. The three "Completed"-badge crons (UW-Cache-Refresh-New, heatmap-warm, Night's-Watch-Warm-New) were all just redeployed ~6 min ago by the same `error-triage RUN 3` push, so their canvas badge read "Completed" with reset Recent-Executions. **Drilled into each one's Cron Runs tab — ALL confirmed WIRED + scheduled, NOT unwired:**
  - **UW-Cache-Refresh-New**: "Next run in 2 min", every 2 min 11am–9:59pm M–F; CRON_SECRET present in Variables; recent executions all green (2–11s).
  - **heatmap-warm**: "Next run in 52s", every minute 11am–9:59pm M–F (Recent Executions empty = redeploy reset, schedule active).
  - **Night's-Watch-Warm-New**: "Next run in 21s", every minute 11am–9:59pm M–F (same redeploy reset; countdown live).
  - Recurring/healthy as before: SPX-Engine-Evaluation + Flow-Ingest-Cron "Next in 16s–1m" (observed counting down live) · NightHawk-Outcomes "Next in 6–7h" · Largo-Chat-CleanUp "Next in 2 days".
- ✅ CROSS-CHECK (live app, RTH, authenticated browser):
  - `/api/market/gex-positioning?ticker=SPX`: spot **7339.95, asof 14:03:09Z (live, seconds old)** — NOT frozen. shift_summary shows intraday evolution ("last 36m: 7450 call wall melted −40.4%, gamma flip migrated down 18.76 pts") = data actively updating. Desk full: flip 7385.93, call_wall 7450, put_wall 7300, max_pain 7445, net_gex −34.8B, gamma short, net_vex +341.9B, vanna positive, source polygon. **heatmap-warm warmer path producing live data → confirms warmers alive even though canvas said "Completed".**
  - `/api/admin/health` (gen 14:02:07Z): Polygon **610 calls/5m, 0 err, 0 ratelimit, last-200**; UW **189 calls/5m, 0 err, 0 ratelimit, last-200**; Anthropic last-200. Rate limiters: UW `circuitOpen=false recent429s=0 degraded=false`, Polygon `circuitOpen=false consecutive429=0`. `redis_degraded=false`, `market_health_ok=true`, `route_errors=[]`. WS: Polygon-indices **OPEN+auth**; all 5 UW channels **OPEN+auth** (ages 137ms–5s); Massive options shard **OPEN+auth** (330ms).
  - ✅ **RESOLVED yesterday's follow-up**: I:SPX index price now **LIVE at 7344.71 (age 792ms)** intraday — the pre-market `price=0` flagged at the 06:30 run was just at-open and populates correctly during RTH, as expected. VIX 19.52 / VIX9D 18.63 / VIX3M 20.74 all fresh.
  - ⚠️ **NEW WATCH-ITEM (minor, non-blocking)**: `health_ok=false` this run — driven SOLELY by **3 "warning" issues: I:TICK / I:TRIN / I:ADD "stale or zero"** (price=0, age=epoch → never received since WS connect). **0 critical.** These are NYSE market-breadth internals; they've apparently never ticked (likely a Polygon-subscription/symbol-feed gap, not a stall — no other symbol is affected). Core SPX/VIX/options/flow data all live. Does NOT affect the main desk, but flips the aggregate `health_ok` flag to false — worth a deliberate decision (either subscribe/fix the I:TICK/TRIN/ADD feed, or downgrade these from health-gating so `health_ok` reflects real critical state). Carry forward to next runs.
- ⏳ NIGHT HAWK EDITION (#77): **not yet due** — 07:03 PT / 10:03 ET; publish window 17:30 ET (≈2:30 PM PT). Will verify on the after-hours run. (NB: 6/25 the edition FAILED to publish ~6h past window — watch closely tonight; note the `fix(nighthawk): harden edition_for date input ::date crash` deploy from ~10 min ago, which targets that exact failure class.)
- VERDICT: **Railway GREEN, live data FRESH.** No #90 (all three "Completed" warmers proven wired via Cron Runs + live downstream gex/health data). No failed/crashed services or failed deploys (active deploy successful; "Building" timer = SDLC churn). No login wall. One minor watch-item: TICK/TRIN/ADD breadth feed never ticking (3 warnings, 0 critical). Yesterday's pre-market SPX-zero follow-up resolved. #77 not yet due.
