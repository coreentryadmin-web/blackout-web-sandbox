# Railway config for Vector-Universe-Snapshot cron.
[build]
builder = "nixpacks"
buildCommand = "echo 'cron trigger service: no app build needed'"
watchPatterns = [
  "scripts/hit-cron.mjs",
  "railway.vector-universe-snapshot.toml",
  "nixpacks.toml",
]

[deploy]
startCommand = "node scripts/hit-cron.mjs /api/cron/vector-universe-snapshot"
cronSchedule = "1-59/5 11-21 * * 1-5"
restartPolicyType = "never"
numReplicas = 1
