# Railway config for the Market Regime Detector cron service.
# Wire it up: create a cron service -> Settings -> Config-as-code -> set path to "railway.market-regime-detector.toml"
# Requires CRON_SECRET on this service (same value as blackout-web).
[build]
builder = "nixpacks"
buildCommand = "echo 'cron trigger service: no app build needed'"

[deploy]
startCommand = "node scripts/hit-cron.mjs /api/cron/market-regime-detector"
# Every 5 minutes across the UTC band that covers RTH in BOTH EDT (UTC-4) and EST (UTC-5):
# 11-21 UTC spans ~7AM-5PM ET. The route self-skips outside isSpxEngineCronWindow and when the
# market is closed, so the extra ticks are cheap no-ops.
cronSchedule = "*/5 11-21 * * 1-5"
restartPolicyType = "never"
numReplicas = 1
