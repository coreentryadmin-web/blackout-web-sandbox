# Railway config for the SPX Issues Sync cron service (admin_incidents reconcile for
# SPX play/engine health — Claude veto, gate blocks/warnings, heartbeat silent/stale).
# Wire it up: create a cron service -> Settings -> Config-as-code -> set path to "railway.spx-issues-sync.toml"
# Requires CRON_SECRET on this service (same value as blackout-web).
[build]
builder = "nixpacks"
buildCommand = "echo 'cron trigger service: no app build needed'"
# Only redeploy this trigger service when its OWN driver/config changes — NOT on docs/src/
# log commits. Without this, EVERY repo commit (incl. the frequent autonomous-SDLC docs/auto
# log pushes) redeployed all cron trigger services, stalling the every-minute warm writers
# ~15-21 min mid-RTH and tripping the cron-staleness watchdog. This service only runs
# scripts/hit-cron.mjs — ALL business logic lives in the main blackout-web app it pings —
# so it only needs to redeploy when hit-cron.mjs or this TOML changes. Mirrors railway.toml's
# proven repo-relative-glob watchPatterns (a leading-slash "/**" matches NOTHING on Railway).
watchPatterns = [
  "scripts/hit-cron.mjs",
  "railway.spx-issues-sync.toml",
  "nixpacks.toml",
]

[deploy]
# Every 5 minutes across the UTC band that covers RTH in BOTH EDT (UTC-4) and EST (UTC-5):
# 11-21 UTC spans ~7AM-5PM ET. Matches railway.spx-evaluate.toml / railway.data-integrity.toml —
# this reconciler reads the same play-engine heartbeat those crons drive, so it needs to catch
# a stale/silent engine on the same cadence they tick on, not slower. The route self-skips
# outside isSpxEngineCronWindow, so the extra ticks are cheap no-ops.
startCommand = "node scripts/hit-cron.mjs /api/cron/spx-issues-sync"
cronSchedule = "*/5 11-21 * * 1-5"
restartPolicyType = "never"
numReplicas = 1
