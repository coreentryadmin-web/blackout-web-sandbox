# [DEPRECATED] Railway has been decommissioned — all infra is on AWS ECS.
# Kept for historical reference; triggers stripped to manual-only.

name: "[DEPRECATED] Railway audit apply"

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Railway has been decommissioned. All infra is on AWS ECS."
